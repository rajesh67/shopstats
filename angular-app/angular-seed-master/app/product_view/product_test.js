'use strict';

describe('shopstats.productView module', function() {

  beforeEach(module('shopstats.productView'));

  describe('productView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('productViewCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});