'use strict';

describe('shopstatsApp.productListView module', function() {

  beforeEach(module('shopstatsApp.productListView'));

  describe('productListView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('productListViewCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});